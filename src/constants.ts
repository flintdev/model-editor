// src/constants.ts

export enum NodeTypeOption {
    string = 'string',
    number = 'number',
    integer = 'integer',
    boolean = 'boolean',
    array = 'array',
    object = 'object',
}

export enum StringFormat {
    date = 'date',
    datetime = 'date-time',
    password = 'password',
    byte = 'byte',
    binary = 'binary'
}